# CIMC 赛题开发顺序与自动测评对应说明

## 1. 先给结论

| 问题 | 结论 |
|---|---|
| 现在应该从哪里开始写 | 从 App 的“协议公共底座 + 参数持久化模块”开始写，不建议先直接写某一个零散命令 |
| 代码是不是完全按自动测评 A~N 顺序写 | 不完全按 A~N 写；开发应按依赖顺序写，验收必须按 A~N 顺序测 |
| 为什么不能直接按 A-01、A-02 往后写 | 自动测评后半段会重启、修改 ID、修改波特率、升级；如果前面没有参数持久化和统一命令分发，后面会连锁失败 |
| 当前工程优先目标 | 先让 A/B/C/D/E/F/G 这些 App 基础功能稳定，再写 H/I/J/K/L/M，最后联调 N Bootloader 升级 |

一句话：**开发顺序按模块依赖走，测试验收按自动测评 A~N 走。**

---

## 2. 题目重新确认到的关键要求

根据 `D:\GD32\2026706296\1 2026年CIMC工业嵌入式系统开发 初赛 赛题.pdf`，自动评测流程是：

| 自动评测阶段 | 题目流程 | 核心风险 |
|---|---|---|
| A | 广播寻址、重启、等待心跳 | 设备 ID、心跳帧、重启后上线必须正确 |
| B | 查询版本、时间、波特率、CH0、CH1、阈值 | 基础命令和数据格式必须完整 |
| C | 设置时间并回读 | RTC 设置、读取、UTC 时间戳必须正确 |
| D/E | 变比、DAC、阈值设置 | float 大端、DAC 0~4095、参数立即生效 |
| F/G | 重启后持久化、DAC 联动 | Flash 参数保存必须可靠，ADC/DAC 回读要正确 |
| H | 自动上报 | 自动上报期间只允许停止命令，其它普通命令不能响应 |
| I | 告警 | 超阈值记录、主动上报、查询、清除 |
| J | 睡眠唤醒 | `0x03AA` 后 RTC 10s 唤醒并直接输出 `instrument wakeup` |
| K | 异常帧处理 | CRC 错、长度错、非法命令都要回错误帧 |
| L | 修改设备 ID | 错 ID 不应答，新 ID 重启后持久化 |
| M | 修改波特率 | 先 OK，再切到 115200；重启后仍保持 115200 |
| N | Bootloader 固件升级 | `0x0501 -> 0x0502 -> bin -> 0x0503`，魔术字 `5AA5C33C` |

题目还明确：

| 项目 | 要求 |
|---|---|
| 默认串口 | USART1/RS485，默认 `19200 8N1` |
| 帧格式 | 先按二进制帧结构组帧，再以 ASCII 十六进制字符串发送；不要用串口工具 Hex 发送 |
| CRC | CRC-16-Modbus，计算范围为帧头到内容区，CRC 大端传输 |
| 部分直接字符串 | 告警查询、Bootloader 提示、睡眠唤醒不封协议帧，直接输出 ASCII 字符串 |
| 持久化参数 | CH0/CH1 变比、CH0/CH1 阈值、设备 ID、波特率、告警记录 |
| 自动测评起始 | 上位机会强制切到 `19200`，所以出厂默认必须是 `19200` |

---

## 3. 当前工程现状

| 模块 | 当前状态 | 下一步判断 |
|---|---|---|
| 工程裁剪 | USART0/USART5、按键、SMARTFS/littlefs、旧 OTA 工具已经删除 | 不要恢复这些旧模块 |
| USART1/RS485 | 已保留为正式通信口，默认 `19200` | 后续所有协议都走这里 |
| 协议框架 | `Protocol/cimc_protocol.c` 已有 ASCII HEX、CRC、组帧、错误帧基础能力 | 需要扩展命令表和公共响应接口 |
| 已有命令 | `0x0301`、`0x0302`、`0x0303`、`0x0501` | 只是最小接入，离完整评分还差很多 |
| LED/OLED | 已裁成两灯、双行显示 | 后续只要跟随自动采集状态即可 |
| ADC/DAC | DAC 已由 `0x0301` 控制，不再被 ADC 直通覆盖 | 需要新增 CH0/CH1 浮点查询和变比 |
| PT100 | 已有 GD30AD3344 PT100 换算任务 | 需要接入 `0x0221` 查询 |
| RTC | 驱动已有设置、读取、秒级计数能力 | 需要接入 `0x0105/0x0106` 和睡眠唤醒 |
| 参数区 | Bootloader 参数区已有读改写能力，含 `user_config[512]` 预留 | 需要设计 App 参数结构并保存到预留区 |
| 告警 | 当前没有正式告警模块 | 需要新建记录、查询、清除、主动上报 |
| Bootloader | 独立工程在 `D:\GD32\2026706296_bootloader` | App 做完基础后再集中联调 N 项 |

---

## 4. 推荐开发顺序

### 4.1 总体顺序

| 顺序 | 先写什么 | 为什么先写 | 对应评分 |
|---:|---|---|---|
| 1 | 协议公共层补齐 | 所有命令都依赖解析、组帧、CRC、错误帧、设备 ID 过滤 | A、K、全流程 |
| 2 | 参数持久化模块 | ID、波特率、变比、阈值、告警都要重启后有效 | F、G、L、M、I |
| 3 | 系统基础命令 | 广播、心跳、重启、版本、ID、波特率 | A、B、L、M |
| 4 | RTC 时间命令 | 设置时间、查询时间、告警时间戳、自动上报时间戳都依赖 RTC | B、C、H、I、J |
| 5 | 采样与数据查询 | CH0、CH1、CH2、变比、DAC 联动是中段大分值 | B、D、G |
| 6 | 阈值和告警 | 需要采样值、RTC、Flash 参数都已有后再写 | E、I |
| 7 | 自动上报状态机 | 需要 CH0/CH1 数据和上报间隔已有后再写 | H |
| 8 | 睡眠 10s 唤醒 | 需要 RTC、外设恢复、RS485 发送都可用 | J |
| 9 | 异常帧和边界补强 | 前面命令多了以后统一校验错误路径 | K、全流程稳定性 |
| 10 | Bootloader 升级联调 | 最破坏性，放最后；否则会影响基础调试效率 | N |

### 4.2 不是按 A~N 直接写的原因

| 自动测评顺序 | 如果直接按它写的问题 | 正确做法 |
|---|---|---|
| A 先测广播和重启 | A-02 重启后马上要心跳，若参数和启动心跳没统一，后续每次重启都会反复出问题 | 先写通用心跳发送、设备 ID 获取、重启后初始化流程 |
| B 立即查很多基础数据 | B 同时用到版本、时间、波特率、CH0/CH1、阈值，不是一个模块 | 先把参数模块、RTC、采样模块分开写好 |
| F/G 才测持久化 | 如果等到 F/G 才写持久化，前面的 D/E/L/M 逻辑会返工 | 一开始就把所有可写参数接入 Flash |
| H/I/J/K 有特殊输出 | 自动上报、告警字符串、睡眠字符串、错误帧输出规则不同 | 协议层要预留“帧输出”和“直接字符串输出”两个出口 |
| M/N 是破坏性测试 | 改波特率和升级失败会导致后续无法通信 | 放最后集中测试，且每次测试前保留可恢复固件 |

---

## 5. 每一步具体怎么写

### 第 1 步：协议公共层

| 文件 | 建议动作 |
|---|---|
| `Protocol/cimc_protocol.h` | 暴露命令字枚举、帧类型、公共发送 OK/错误/普通应答接口 |
| `Protocol/cimc_protocol.c` | 保留现有 ASCII HEX 和 CRC 逻辑，补齐心跳帧、广播帧、错误帧、float 大端写入 helper |
| `Function/usart_app.c` | 继续只负责 ISR 帧缓存转交，不在这里写业务 |

必须先保证：

| 能力 | 验证方式 |
|---|---|
| 广播 `0xFFFF` 帧类型 `0x05` 能回 `0x8888` 心跳帧 | 对应 A-01 |
| CRC 错误返回 `0xFF/0xEEEE` 错误帧 | 对应 K-01 |
| 非本机 ID 静默丢弃 | 对应 L-01 |

### 第 2 步：参数持久化模块

建议新增：

| 新文件 | 作用 |
|---|---|
| `Function/cimc_params.h` | 定义设备 ID、波特率、变比、阈值、上报间隔、告警主动上报开关 |
| `Function/cimc_params.c` | 从内部 Flash 参数区加载、默认值填充、CRC 校验、写回 |

推荐默认值：

| 参数 | 默认值 |
|---|---|
| 固件版本 | `2.0.1.0` |
| 设备 ID | `0x0001`，后续允许 `0x0001~0xFFFE` |
| 波特率 | `19200`，映射值 `0x13` |
| CH0/CH1 变比 | 先用 `1.0f` |
| CH0/CH1 阈值 | 先用一个安全默认值，后续按上位机写入覆盖 |
| 上报间隔 | `1s`，映射值 `0x01` |
| 告警主动上报 | 默认关闭或按题目上位机写入生效 |

注意：当前 Bootloader 参数结构里有 `user_config[512]`，可以优先把 App 参数放这里；不要破坏前 256 字节 Bootloader 字段。

### 第 3 步：系统基础命令

| 命令 | 作用 | 写法 |
|---|---|---|
| `0x0101` | 重启 | 先回 OK，延时 20ms，再 `NVIC_SystemReset()` |
| `0x0104` | 查询固件版本 | 返回 4 字节版本，例如 `02 00 01 00` 表示 `2.0.1.0` |
| `0x0111` | 查询设备 ID | 广播地址下发，返回当前设备 ID |
| `0x0112` | 查询波特率 | 返回映射值，默认 `0x13` |
| `0x01A1` | 设置设备 ID | 校验 `0001~FFFE`，写 Flash，返回新 ID 下的 OK |
| `0x01A2` | 设置波特率 | 校验 `11/12/13/14`，先回 OK，再切换串口并持久化 |

这一阶段先覆盖 A、B、L、M 的系统部分，但 M 的 115200 实测放到最后，因为它会改变通信条件。

### 第 4 步：RTC 时间命令

| 命令 | 作用 | 写法 |
|---|---|---|
| `0x0105` | 设置时间 | 接收 UTC 秒级时间戳，转换成 RTC 年月日时分秒后写入 |
| `0x0106` | 查询时间 | 读取 RTC，返回 UTC 秒级时间戳 |

当前 `Driver/RTC/bsp_rtc.c` 已有 `bsp_rtc_set_datetime()`、`bsp_rtc_get_datetime()`、`bsp_rtc_get_epoch_seconds()`，可以复用。若题目使用 Unix UTC 秒，建议新增明确的 Unix epoch 转换 helper，不要混用当前 2000 年起算的内部 sleep 计数。

### 第 5 步：采样、变比、DAC

建议新增：

| 新文件 | 作用 |
|---|---|
| `Function/cimc_sample.h` | 定义 CH0/CH1/CH2 查询接口 |
| `Function/cimc_sample.c` | 读取 ADC DMA、PT100 缓存，乘以变比后输出 float |

命令实现：

| 命令 | 数据 |
|---|---|
| `0x0201` | CH0 float 大端，`ADC0_CH0 * CH0_ratio` |
| `0x0202` | CH1 float 大端，`ADC0_CH1 * CH1_ratio` |
| `0x0221` | CH2 PT100 温度 float 大端 |
| `0x0241` | 设置 CH0 变比 float 大端，立即生效并持久化 |
| `0x0242` | 设置 CH1 变比 float 大端，立即生效并持久化 |
| `0x0301` | 已有 DAC 设置，继续保留 |

注意：CH0/CH1 题目要求“原始采样值 × 当前变比”，不要擅自转成电压后再乘，除非上位机明确按电压判定。

### 第 6 步：阈值与告警

建议新增：

| 新文件 | 作用 |
|---|---|
| `Function/cimc_alarm.h` | 告警结构、主动上报开关、查询/清除接口 |
| `Function/cimc_alarm.c` | 最近 10 条告警，Flash 持久化，字符串格式输出 |

命令实现：

| 命令 | 写法 |
|---|---|
| `0x0400` | 返回 CH0、CH1 两个阈值 float 大端 |
| `0x0401` / `0x0402` | 分别返回 CH0/CH1 阈值 |
| `0x0411` / `0x0412` | 写入 CH0/CH1 阈值，持久化 |
| `0x0601` | 设置是否主动上报告警：`01` 主动，`02` 仅记录 |
| `0x0602` | 直接字符串输出最近 10 条，空则输出 `empty` |
| `0x0603` | 清除告警，回 OK |

告警字符串格式必须是题目要求的：

```text
2026-01-01 12:00:00 | CH0 | 10.50 | 11.12
```

### 第 7 步：自动上报

建议新增：

| 新文件 | 作用 |
|---|---|
| `Function/cimc_auto_report.h` | 自动上报状态和间隔接口 |
| `Function/cimc_auto_report.c` | 1s/3s/5s 定时上报 CH0/CH1 |

命令实现：

| 命令 | 写法 |
|---|---|
| `0x0261` | 设置上报间隔：`01=1s`，`02=3s`，`03=5s` |
| `0x0302` | 开始自动上报，立即回一帧 12 字节数据，后续按间隔继续发 |
| `0x0303` | 停止自动上报，回 OK |

关键规则：

| 规则 | 实现要求 |
|---|---|
| 自动上报期间只响应停止命令 | `cimc_protocol_dispatch` 中先判断自动上报状态，非 `0x0303` 普通命令不回包 |
| OLED/LED 跟随状态 | `cimc_status_set_auto_sample()` 继续作为统一状态源 |
| 上报内容 | `UTC 秒级时间戳 + CH0 float + CH1 float`，共 12 字节 |

### 第 8 步：睡眠 10s 唤醒

建议新增：

| 新文件 | 作用 |
|---|---|
| `Driver/POWER/bsp_cimc_power.c/h` | 只实现赛题 RTC 10s 睡眠，不恢复旧 `bsp_power` 按键低功耗演示 |
| `Function/cimc_power_app.c/h` | 协议命令入口和唤醒后字符串输出状态 |

命令：

| 命令 | 写法 |
|---|---|
| `0x03AA` | 回 OK，关闭不必要外设，RTC 闹钟 10s，进入深度睡眠，唤醒后恢复 USART1/RS485 并直接输出 `instrument wakeup` |

注意：不要恢复 KEYW/按键唤醒逻辑，题目要求 RTC 10s 自动唤醒。

### 第 9 步：异常帧统一补强

| 测评项 | 要求 |
|---|---|
| K-01 CRC 错误帧 | 返回错误应答帧 |
| K-02 长度错误帧 | 返回错误应答帧 |
| K-03 非法命令字 | 返回错误应答帧 |

建议把错误出口统一成：

| 场景 | 行为 |
|---|---|
| ID 不匹配 | 静默丢弃 |
| 广播查询 | 用本机 ID 回心跳或设备 ID |
| CRC/长度/未知帧类型 | `0xFF / 0xEEEE` |
| 已解析到命令但参数非法 | `0xFF / 原命令字` 或按题目要求错误帧 |

### 第 10 步：Bootloader N 项联调

App 侧先确保 `0x0501`：

| 项目 | 要求 |
|---|---|
| App 收到 `0x0501` | 回 OK |
| 参数区 | 写 `magicWord`、`updateFlag=0x5A`、`updateStatus=0x02` |
| 复位 | 延时后软件复位进入 Bootloader |

Bootloader 侧集中联调：

| 命令 | 要求 |
|---|---|
| `0x0502` | 回 OK 或错误帧，随后接收 256B 切片 bin |
| bin 魔术字 | 前 4 字节必须是 `5AA5C33C` |
| `0x0503` | 先回 OK，再执行搬运、校验、回滚 |
| 普通上电 | 无升级请求时静默 5s 跳 App |
| App 请求升级后 | 打印题目要求的 Bootloader 等待字符串，最多等 10s |

---

## 6. 建议的开发验收节奏

每完成一阶段就做一次小闭环，不要等所有命令写完才测。

| 阶段 | 最小验收 |
|---:|---|
| 1 | 手工发广播帧，能回心跳；发 CRC 错帧，能回错误帧 |
| 2 | 设置 ID/波特率/变比/阈值后断电或复位，参数仍在 |
| 3 | A-01~B-03 手工通过：广播、重启心跳、版本、时间、波特率 |
| 4 | B-04~G-01 手工通过：CH0/CH1/CH2、变比、阈值、DAC 回读 |
| 5 | H/I 手工通过：自动上报、停止、告警记录 |
| 6 | J/K 手工通过：睡眠唤醒、错误帧 |
| 7 | L/M 手工通过：改 ID、改波特率、重启持久化 |
| 8 | N 手工通过：Bootloader 错误固件拒绝、正确固件升级 |
| 9 | 上位机自动测评 A~N 完整跑一遍 |

---

## 7. 当前最应该先写的文件清单

按现在工程状态，下一步优先级如下：

| 优先级 | 文件 | 目标 |
|---:|---|---|
| 1 | `Function/cimc_params.c/h` | 设备 ID、波特率、变比、阈值、上报间隔、告警开关的默认值和持久化 |
| 2 | `Protocol/cimc_protocol.c/h` | 扩展命令字和公共组帧接口，先补 A/B/K 的基础命令 |
| 3 | `Function/cimc_sample.c/h` | CH0/CH1/CH2 查询和 float 大端输出 |
| 4 | `Function/cimc_alarm.c/h` | 阈值判断、最近 10 条告警、直接字符串输出 |
| 5 | `Function/cimc_auto_report.c/h` | 自动上报定时状态机 |
| 6 | `Driver/POWER/bsp_cimc_power.c/h` + `Function/cimc_power_app.c/h` | `0x03AA` RTC 10s 睡眠唤醒 |
| 7 | `D:\GD32\2026706296_bootloader` | 最后联调 Bootloader `0x0502/0x0503` |

---

## 8. 推荐任务拆分

| 任务包 | 内容 | 完成标准 |
|---|---|---|
| Task 1 | `cimc_params` + 系统命令 A/B/L/M 基础 | A-01、A-02、A-03、B-01、B-03、L-01 可手工通过 |
| Task 2 | RTC 时间命令 | B-02、C-01 可手工通过 |
| Task 3 | `cimc_sample` + CH0/CH1/CH2 + 变比 + DAC | B-04、B-05、D-01、D-02、G-01 可手工通过 |
| Task 4 | 阈值和持久化 | B-06、E-01、E-02、F-01~F-04 可手工通过 |
| Task 5 | 自动上报 | H-01、H-02、H-03 可手工通过 |
| Task 6 | 告警 | I-01~I-04 可手工通过 |
| Task 7 | 睡眠和异常帧 | J-01、K-01~K-03 可手工通过 |
| Task 8 | 波特率破坏性实测 | M-01、M-02 可手工通过 |
| Task 9 | Bootloader 升级 | N-01~N-03 可手工通过 |

---

## 9. 最终建议

| 建议 | 原因 |
|---|---|
| 开发不要完全按 A~N 顺序写 | A~N 是上位机评分流程，不是代码依赖顺序 |
| 验收必须按 A~N 顺序测 | 自动测评会严格按 A~N 连续执行，中间不能手动修复 |
| 先写参数持久化 | 后续 ID、波特率、变比、阈值、告警都依赖它 |
| 先写 App，最后写 Bootloader 联调 | Bootloader 升级是破坏性测试，基础协议不稳定时先测它会浪费时间 |
| 每写完一个任务包就 Keil 构建和手工发帧 | 赛题协议错误通常是字节序、CRC、长度和状态机问题，越早抓越省时间 |
