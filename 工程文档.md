# GD32F470VET6 主控板工程详细文档

## 1. 工程概述

本工程基于 **MICU GD32F470VET6 V2** 主控板，使用 GD32F4xx 标准外设库开发，采用 AC6 (ARMCLANG) 编译器。当前工程已按独立 BootLoader 工程 `D:\GD32\2026706296_bootloader` 规划为 **BootLoader App**，链接地址为 `0x0800D000`，用于由 BootLoader 跳转运行。

### 1.1 硬件信息

| 项目 | 参数 |
|------|------|
| 芯片型号 | GD32F470VE |
| 内核 | Cortex-M4, 最高 200MHz |
| Flash | 512KB (0x08000000 - 0x08080000) |
| SRAM | 192KB (0x20000000 - 0x20030000) + 64KB (0x10000000 - 0x10010000) |
| 调试接口 | SWD (PA13/PA14) |

### 1.1.1 BootLoader Flash 分区

| 分区 | 起始地址 | 大小 | 作用 |
|------|----------|------|------|
| BootLoader 区 | 0x08000000 | 48KB | 存放官方 BootLoader，MCU 复位后先从这里启动 |
| 参数区 | 0x0800C000 | 4KB | 存放升级标志、App 大小、CRC、入口地址等参数 |
| App 运行区 | 0x0800D000 | 152KB | 当前工程链接和运行地址，BootLoader 跳转到这里 |
| App 备份区 | 0x08033000 | 152KB | BootLoader 升级前备份旧 App，搬运失败时恢复 |
| App 缓存区 | 0x08059000 | 152KB | 当前工程用于暂存 RS485/USART1 接收并校验后的新 App 镜像 |
| 预留页 | 0x0807F000 | 4KB | 当前不用，作为保护和后续扩展预留 |

### 1.2 板载资源

- 6 路用户 LED (PD10-PD15, 高电平点亮)
- 7 路用户按键 (KEY1-KEY6 + KEYW 唤醒键)
- 0.91 英寸 SSD1306 OLED (I2C0, PB8/PB9)
- GD25Qxx SPI Flash (SPI0, PB3-PB5, PA15)
- GD30AD3344 SPI ADC (SPI3, PE12-PE14, PE10)
- 4 路串口 (USART0/1/2/5)
- 2 路 ADC 通道 (PC0/PC2)
- 1 路 DAC 输出 (PA4)
- RTC 实时时钟 (外部 32.768kHz 晶振)
- 以太网 RMII 接口 (预留)

---

## 2. 工程目录结构

```
2026706296/
├── Function/               # 应用逻辑层代码，保留当前 scheduler.c 调度框架
│   ├── led_app.c/h         # LED 显示任务
│   ├── btn_app.c/h         # 按键处理任务
│   ├── oled_app.c/h        # OLED 显示任务
│   ├── usart_app.c/h       # USART0 SMARTFS 调试命令任务
│   ├── uart_ota_app.c/h    # RS485/USART1 头部 BIN 裸流 OTA 升级入口
│   ├── adc_app.c/h         # ADC 采集任务
│   ├── gd30ad3344_pt100_app.c/h # GD30AD3344 PT100 温度换算任务
│   ├── rtc_app.c/h         # RTC 时间显示任务
│   └── scheduler.c/h       # 简易任务调度器
├── HardWare/               # 板级驱动和器件驱动
│   ├── LED/                # LED GPIO 驱动
│   ├── KEY/                # 按键 GPIO / WKUP EXTI 驱动
│   ├── USART/              # USART0、RS485/USART1、USART5 驱动
│   ├── OLED/               # OLED BSP 与 SSD1306 驱动
│   ├── STORAGE/            # 存储初始化封装
│   ├── ANALOG/             # ADC / DAC 驱动
│   ├── RTC/                # RTC 驱动
│   ├── POWER/              # 深度睡眠和唤醒恢复
│   ├── BOOTLOADER/         # App 与 BootLoader 交接层
│   ├── GD25QXX/            # SPI Flash 驱动 + SMARTFS
│   └── GD30AD3344/         # SPI ADC 驱动
├── HeaderFiles/            # 公共聚合头文件
│   └── system_all.h
├── Library/                # 外部库
│   └── GD32F4xx_standard_peripheral/  # GD32F4xx 标准外设库
├── CMSIS/                  # ARM CMSIS Core 与 GD32F4xx 设备文件
├── Startup/                # Keil 启动汇编文件
├── User/                   # 运行基础设施：main、systick、中断、App 地址接管
├── project/                # Keil 工程、RTE、Objects、output/Listings
└── tools/                  # 上位机辅助脚本
```

Keil 工程树中的底层源码统一放在一个 `HardWare` 分组下显示，不再拆成
`HardWare/OLED`、`HardWare/GD25QXX` 等多个分组。
磁盘上的 `HardWare/<模块名>/` 子目录仍然保留，用于区分驱动和器件归属。

---

## 3. USER 目录文件详解

### 3.1 main.c - 程序入口

**文件路径**: `User/main.c`

**功能**: 系统主函数，负责初始化所有外设并启动任务调度器。

**启动流程**:

```
1. boot_app_handoff_init()   - 接管 BootLoader 跳转现场，恢复 App 向量表和全局中断状态
2. bsp_usart_init()          - 初始化 USART0 调试串口 + USART1/RS485，保证后续启动日志可见
3. systick_config()          - 配置本地 SysTick timebase (1ms 中断) 并启用 DWT 微秒延时基准
4. delay_ms(200)             - 等待 200ms (防止 SWD 引脚被复用为 GPIO 时无法下载)
5. bsp_led_init()            - 初始化 6 路 LED GPIO
6. bsp_btn_init()            - 初始化 7 路按键 GPIO
7. bsp_oled_init()           - 初始化 I2C0 + DMA (OLED 接口)
8. bsp_gd25qxx_init()        - 初始化 SPI0 (SPI Flash 接口)
9. uart_ota_reset_runtime()  - 清空 RS485/USART1 OTA 运行态
10. bsp_gd30ad3344_init()    - 初始化 SPI3 (外部 ADC 接口)
11. gd30ad3344_pt100_app_init() - 初始化 PT100 温度换算缓存，并丢弃首帧 ADC 旧样本
12. bsp_adc_init()           - 初始化 ADC0 (内部 ADC, 2 通道 DMA)
13. bsp_dac_init()           - 初始化 DAC0 + TIMER5 (DAC 输出)
14. bsp_rtc_init()           - 初始化 RTC 实时时钟；若 VBAT 备份域有效则恢复已有时间
15. app_btn_init()           - 初始化按键扫描状态
16. OLED_Init()              - 初始化 SSD1306 OLED 显示屏
17. smart_storage_self_test() - 默认执行 SPI Flash SMARTFS 初始化/文件读写自检
18. test_spi_flash()          - 可选破坏性裸 Flash 读写测试，默认关闭，避免破坏 SMARTFS
19. scheduler_init()         - 初始化任务调度器
20. uart_ota_emit_startup_probe() - 调度器就绪后通过 RS485 输出 `OTA485: ready`，此时可立即裸发 `Project_ota.bin`
21. while(1) scheduler_run() - 主循环运行任务调度
```

**printf 重定向**:
- 将 `fputc()` 重定向到 USART0，实现 `printf()` 输出到调试串口
- GCC 环境下使用 `__io_putchar()` 重定向

---

### 3.2 gd32f4xx_it.c - 中断服务函数

**文件路径**: `User/gd32f4xx_it.c`

**功能**: 定义所有中断服务函数 (ISR)。

| 中断函数 | 触发源 | 功能 |
|----------|--------|------|
| `NMI_Handler` | NMI 异常 | 死循环 (调试用) |
| `HardFault_Handler` | 硬件错误 | 死循环 (调试用) |
| `MemManage_Handler` | 内存管理错误 | 死循环 |
| `BusFault_Handler` | 总线错误 | 死循环 |
| `UsageFault_Handler` | 用法错误 | 死循环 |
| `SVC_Handler` | 系统服务调用 | 死循环 |
| `DebugMon_Handler` | 调试监视器 | 死循环 |
| `PendSV_Handler` | 可挂起系统调用 | 死循环 |
| `SysTick_Handler` | SysTick 定时器 | 调用 `systick_tick_inc()` 推进本地 1ms timebase |
| `USART0_IRQHandler` | USART0 空闲中断 | DMA 接收一帧 SMARTFS 调试命令，设置 `rx_flag` 标志 |
| `USART1_IRQHandler` | USART1/RS485 空闲中断 | DMA 接收一段 OTA 裸流数据，写入 OTA 环形队列并设置 `uart_ota_rx_flag` 标志 |
| `EXTI0_IRQHandler` | EXTI0 外部中断 | 清除中断标志 (唤醒键) |

**USART0 接收流程**:
1. 检测到空闲帧中断 (IDLE)
2. 读取 DR 寄存器清除 IDLE 标志
3. 关闭 DMA 通道
4. 计算接收到的数据长度
5. 将本次 IDLE 帧数据复制到 `uart_dma_buffer`
6. 记录 `uart_dma_length`，并设置 `rx_flag = 1` 通知应用层
7. 重新配置 DMA 继续接收

**USART1/RS485 OTA 接收流程**:
1. 检测到 `USART1` 空闲帧中断 (IDLE)
2. 读取 DR 寄存器清除 IDLE 标志
3. 关闭 DMA 通道并计算本帧有效长度
4. 更新 `uart_ota_irq_count`、`uart_ota_last_irq_length` 和队列满丢段计数，便于 USART0 日志诊断
5. 将本次数据段复制到 `uart_ota_dma_buffer[write_index]`
6. 记录 `uart_ota_dma_length[write_index]`，推进环形队列写索引，并设置 `uart_ota_rx_flag = 1` 通知 `uart_ota_task()`
7. 重新配置 USART1 DMA 继续接收下一段 OTA 数据

---

### 3.3 systick.c - SysTick 定时器配置

**文件路径**: `User/systick.c`

**功能**: 配置 SysTick 定时器产生 1ms 中断，提供本地 timebase、阻塞延时和 DWT 微秒辅助功能。

| 函数 | 功能 |
|------|------|
| `systick_config()` | 配置 SysTick 为 1000Hz 中断 (1ms)，同时初始化 DWT 微秒忙等基准 |
| `systick_tick_inc()` | 在 SysTick 中断中调用，推进本地毫秒 tick |
| `timebase_get_ms32()` | 快速读取当前 32 位毫秒 tick，供调度器使用 |
| `get_system_ms()` | 读取 64 位累计毫秒时间戳 |
| `get_system_us()` | 读取累计微秒时间戳，兼容旧接口，并补偿 SysTick 溢出但中断未执行的边界窗口 |
| `delay_ms(ms)` | 毫秒级阻塞延时，正常路径基于毫秒 tick 差值 |
| `delay_us(us)` | 微秒级阻塞延时，基于 DWT 周期计数 |
| `delay_1ms(count)` | 兼容旧接口，内部转调 `delay_ms()` |
| `delay_decrement()` | 兼容旧接口，当前保持空实现 |
| `timebase_prepare_reconfiguration()` | 深睡或重配 SysTick 前收口本地 timebase |
| `timebase_update_after_clock_change()` | 时钟变化或深睡唤醒后恢复本地 timebase |
| `timebase_adjust_ms(elapsed_ms)` | RTC 补偿入口；深睡唤醒后把 RTC 秒差追加到本地毫秒 timebase |

**注意**: 当前工程不再依赖独立的 `delay` 递减变量；毫秒节拍由 `systick_tick_inc()` 维护，微秒延时由 DWT 周期计数器提供。DWT 微秒换算按主频向上取整，优先保证阻塞延时不短于调用方要求。深睡期间 SysTick 停止，唤醒后通过 RTC 补偿把睡眠秒差追加到 timebase；该补偿用于日志时间和跨睡眠超时基准，RTC 仍是墙上时间来源。

---

### 3.4 main.h - 主程序头文件

**文件路径**: `User/main.h`

**功能**: 主程序头文件，当前为空（仅包含头文件保护宏）。可在此添加全局宏定义。

---

### 3.5 systick.h - SysTick 头文件

**文件路径**: `User/systick.h`

**功能**: 声明 SysTick 相关函数原型。

---

### 3.6 gd32f4xx_it.h - 中断服务函数头文件

**文件路径**: `User/gd32f4xx_it.h`

**功能**: 声明所有中断服务函数原型。

---

### 3.7 gd32f4xx_libopt.h - 外设库选择头文件

**文件路径**: `User/gd32f4xx_libopt.h`

**功能**: 根据芯片型号条件包含所需的外设库头文件。通过 `#ifdef GD32F470` 宏自动包含 GD32F470 系列所需的全部外设库。

---

## 4. Function 目录文件详解

### 4.1 scheduler.c/h - 任务调度器

**文件路径**: `Function/scheduler.c`, `Function/scheduler.h`

**功能**: 实现一个基于时间片的简易协作式任务调度器。

**调度器架构**:

```c
typedef struct {
    void (*task_func)(void);  // 任务函数指针
    uint32_t rate_ms;         // 执行周期 (ms)
    uint32_t last_run;        // 上次执行时间 (ms)
} task_t;
```

**注册的任务列表**:

| 任务函数 | 执行周期 | 功能 |
|----------|----------|------|
| `led_task` | 1ms | LED 显示刷新 |
| `adc_task` | 100ms | ADC 数据更新 |
| `gd30ad3344_pt100_task` | 200ms | 读取 GD30AD3344 并换算 PT100 电阻和温度 |
| `oled_task` | 100ms | OLED 显示刷新 |
| `btn_task` | 5ms | 按键扫描处理 |
| `uart_task` | 5ms | USART0 SMARTFS 调试命令处理 |
| `uart_ota_task` | 5ms | RS485/USART1 OTA 分包处理 |
| `rtc_task` | 500ms | RTC 时间显示更新 |

**调度原理**:
1. `scheduler_init()`: 计算任务数量
2. `scheduler_run()`: 先读取一次 `timebase_get_ms32()`，再遍历任务表
3. 使用 `(uint32_t)(now_time - last_run) >= rate_ms` 判断任务是否到期，兼容 32 位 tick 回绕
4. 到期任务立即执行，更新 `last_run` 时间戳
5. `scheduler_reset_runtime()`: 深睡唤醒并完成外设恢复后执行调度器唤醒重基线，把所有任务 `last_run` 设置为当前 tick，避免 OLED/UART/RTC/ADC 在恢复后的第一轮集中执行

**添加新任务方法**:
1. 在 `Function/` 目录创建 `xxx_app.c/h`
2. 实现任务函数 `void xxx_task(void)`
3. 在 `scheduler.c` 的 `scheduler_task[]` 数组中添加条目

---

### 4.2 led_app.c/h - LED 显示任务

**文件路径**: `Function/led_app.c`, `Function/led_app.h`

**功能**: 控制 6 路 LED 的显示状态。

**全局变量**:
- `ucLed[6]`: LED 状态数组，1 = 亮，0 = 灭。初始值 `{1,0,1,0,1,0}` (交替亮灭)

**函数说明**:

| 函数 | 功能 |
|------|------|
| `led_disp(const uint8_t *ucLed)` | 根据状态数组控制 LED，带变化位检测优化 |
| `led_app_set(index, state)` | 通过应用层状态源设置单个 LED，并立即同步硬件 |
| `led_app_toggle(index)` | 通过应用层状态源翻转单个 LED，并立即同步硬件 |
| `led_app_all_off()` | 将 `ucLed[6]` 全部置 0，并同步关闭 6 路 LED |
| `led_app_blank_for_sleep()` | 低功耗入口熄灭 LED，并让 LED 刷新缓存失效 |
| `led_app_reset_cache()` | 复位 LED 硬件刷新缓存，唤醒后下一轮强制重刷 |
| `led_task()` | 调度器调用的任务函数 |

**LED 控制逻辑**:
- **LED 应用层状态源**: 运行态按键和低功耗入口统一通过 `led_app_set()`、`led_app_toggle()`、`led_app_all_off()` 修改 `ucLed[6]`，不再直接 `LEDx_TOGGLE` 绕过状态缓存。
- 遍历 `ucLed[6]` 数组，将状态编码为 6 位 `led_mask` 变量
- 首次刷新时强制写入 6 路 LED，确保上电默认关闭状态能同步为应用层状态
- 后续刷新与上次状态 `led_mask_old` 异或得到 `changed_mask`，无变化时直接返回
- 只对 `changed_mask` 中置位的 LED 调用 `LED1_SET()` ~ `LED6_SET()` 宏，避免单个 LED 变化时重写全部引脚
- 深睡/Standby 关闭 GPIO 或重新初始化 LED 后，会调用 `led_app_reset_cache()`，避免硬件已被熄灭但应用层缓存误判“无需刷新”。

---

### 4.3 btn_app.c/h - 按键处理任务

**文件路径**: `Function/btn_app.c`, `Function/btn_app.h`

**功能**: 使用简单轮询 + 按下沿检测实现 7 路按键处理。

**按键定义**:

| 按键 ID | 物理按键 | GPIO | 点击动作 |
|---------|----------|------|----------|
| KEY1 | KEY1 | PB1 | LED1 翻转 + 进入 Sleep |
| KEY2 | KEY2 | PC5 | LED2 翻转 + 进入 Deep-sleep |
| KEY3 | KEY3 | PC4 | LED3 翻转 + 进入 Standby |
| KEY4 | KEY4 | PA7 | 普通运行态 LED4 翻转；KEY3 Standby 准备态下按下后松开用于确认进入 |
| KEY5 | KEY5 | PA6 | LED5 翻转 |
| KEY6 | KEY6 | PA5 | LED6 翻转 |
| KEYW | KEYW | PA0 | LED6 翻转 |

**实现说明**:
- `btn_task()` 以 5ms 周期运行
- 模块内部使用 20ms 轻量软件去抖
- 仅对按下沿触发动作，长按期间不会重复执行
- `KEYW` 在普通运行态作为普通按键使用，在 Sleep/Deep-sleep 阶段由 `EXTI0` 下降沿作为唤醒源
- Standby 阶段使用 KEYW/PA0 的 PMU WKUP 功能唤醒；KEY3 进入准备态后先熄 OLED，并通过 `led_app_all_off()` 关闭 LED 应用层状态，再等待 KEY4 按下后松开作为确认，KEYW 只保留为唤醒键

**函数说明**:

| 函数 | 功能 |
|------|------|
| `app_btn_init()` | 初始化按键扫描状态，建立当前稳定状态基线 |
| `btn_task()` | 轮询按键、做轻量去抖并分发按下沿动作 |
| `prv_btn_read_mask()` | 读取 7 个物理按键并编码为按键位图 |
| `prv_btn_dispatch()` | 根据按下沿位图调用 `led_app_toggle()` 或三档低功耗动作 |

---

### 4.4 oled_app.c/h - OLED 显示任务

**文件路径**: `Function/oled_app.c`, `Function/oled_app.h`

**功能**: 在 0.91 英寸 OLED 上显示系统状态信息。

**显示内容**:
- 第 0 行: 6 路按键状态 (KEY1-KEY6)
- 第 1 行: 系统毫秒时间戳 (`uwTick`)
- 第 2 行: ADC 通道 0 电压值和内部参考电压
- 第 3 行: RTC 当前时间 (由 `rtc_task` 更新)

**函数说明**:

| 函数 | 功能 |
|------|------|
| `oled_printf(x, y, format, ...)` | 类似 printf 的 OLED 显示函数，支持格式化字符串 |
| `oled_task()` | 调度器调用的任务函数，更新 OLED 显示内容 |

**实现细节**:
- 使用 `OLED_APP_LINE_BUFFER_SIZE=32` 的行级栈缓冲执行 `vsnprintf()`，避免 OLED 周期任务长期占用 512 字节临时缓冲。
- `g_oled_line_cache` 缓存 4 行已经显示的文本；`oled_printf_diff_start()` 和 `oled_printf_diff_end()` 会定位首尾变化字符，只把变化段交给 `OLED_ShowStr()`，减少 100ms 周期任务里的 I2C/DMA 写入。
- 每行按 128 像素宽度裁剪到 16 个 8 像素字符，并补空格覆盖旧长字符串残留。
- 调用 `OLED_ShowStr()` 显示 6x8 ASCII 字符；**OLED 失败不上缓存**，只有底层 I2C/DMA 返回成功后才更新 `g_oled_line_cache`，屏幕离线或总线超时时下一轮仍会重试同一差异段。
- X 轴范围: 0-127, Y 轴范围: 0-3 (共 4 行)。

---

### 4.5 usart_app.c/h - USART0 SMARTFS 调试任务

**文件路径**: `Function/usart_app.c`, `Function/usart_app.h`

**功能**: 提供串口打印，并把 `USART0` 作为 SMARTFS 调试口，提供类 Linux 的文件系统命令壳层，支持目录切换、目录列举、文件读取、文件覆盖写入、建目录、建空文件和容量查询。

**全局变量**:
- `rx_flag`: 接收完成标志 (在 ISR 中置 1)
- `uart_dma_buffer[1024]`: USART0 单帧接收缓冲区，用于接收一条 SMARTFS 调试命令
- `uart_dma_length`: 本次 USART0 IDLE 帧的有效字节数

**函数说明**:

| 函数 | 功能 |
|------|------|
| `my_printf(usart_periph, format, ...)` | 支持任意串口的 printf 函数 |
| `uart_task()` | 检查 `rx_flag`，解析 `help/gettime/rtcstat/settime/pwd/ls/cd/cat/write/mkdir/touch/rm/stat/df` 命令，并执行 SMARTFS 调试或 RTC 读写操作 |

**USART0 调试命令**:

| 命令 | 功能 |
|------|------|
| `help` | 打印当前支持的命令列表 |
| `gettime` | 读取当前 RTC 年月日时分秒，回显格式为 `RTC: YYYY-MM-DD HH:MM:SS` |
| `rtcstat` | 读取 RTC 时钟源、备份域、分频和校准寄存器，回显格式包含 `src/ready/bkp/recovered/psc_a/psc_s/bdctl/hrfc/cosc` |
| `settime <yyyy-mm-dd> <hh:mm:ss>` | 设置当前 RTC 年月日时分秒；成功后回显 `RTC: SET OK YYYY-MM-DD HH:MM:SS` |
| `pwd` | 打印当前工作目录 |
| `ls [path]` | 列出当前目录或指定目录下的文件和子目录；数字列统一表示大小，目录为其目录树下全部文件内容总字节数 |
| `cd <path>` | 切换当前工作目录，支持相对路径和绝对路径 |
| `cat <file>` | 读取指定文件并把内容回显到 USART0 |
| `write [-a] <file> <文本>` | 默认把 `<文本>` 覆盖写入指定文件；带 `-a` 时把 `<文本>` 追加到文件尾部，随后重新读回验证并回显 |
| `mkdir <dir>` | 创建目录；父目录不存在时返回错误 |
| `touch <file>` | 创建空文件；文件已存在时保持原样返回成功 |
| `rm <path>` | 删除文件；若目标是目录，则递归删除整个目录树；目标不存在时报错，根目录 `/` 禁止删除 |
| `stat [path]` | 查询当前路径或指定路径的类型和大小；文件与目录都显示 `size`，目录大小为其目录树下全部文件内容总字节数 |
| `df` | 打印 SMARTFS 总容量、已用容量、空闲容量和当前工作目录 |

**my_printf 实现**:
1. 使用 `vsnprintf()` 格式化字符串到 512 字节缓冲区
2. 逐字节发送到指定串口
3. 等待发送完成 (TBE 标志)
4. 返回实际发送长度

---

### 4.6 uart_ota_app.c/h - OTA 升级任务

**文件路径**: `Function/uart_ota_app.c`, `Function/uart_ota_app.h`

**功能**: 提供 `RS485/USART1` 专用头部 BIN 裸流 OTA 接收、App 缓存区写入和 BootLoader 交接流程。当前现场升级文件固定为 `project/output/Project_ota.bin`，格式为 64 字节 OTA 头部 + 原始 App payload。

**全局变量**:
- `uart_ota_rx_flag`: OTA 接收队列非空标志 (在 `USART1_IRQHandler` 或 `DMA0_Channel5_IRQHandler` 中置 1)
- `uart_ota_dma_buffer[4][1024]`: RS485/USART1 DMA 裸流移交环形队列，用于分段接收连续发送的 `Project_ota.bin`
- `uart_ota_dma_length[4]`: 每个队列槽的有效字节数
- `uart_ota_queue_write_index/read_index/count`: ISR 和任务层之间的环形队列写索引、读索引和当前槽数

**函数说明**:

| 函数 | 功能 |
|------|------|
| `uart_ota_reset_runtime()` | 清空 OTA 会话状态、共享长度和私有缓冲区 |
| `uart_ota_emit_startup_probe()` | 在启动自检和 `scheduler_init()` 完成后，通过 RS485/USART1 输出 `OTA485: ready, send Project_ota.bin raw`，表示 `uart_ota_task()` 已具备消费 DMA 队列的条件 |
| `uart_ota_feed_rx_bytes()` | 把 DMA/IDLE 捕获到的连续字节喂给头部 BIN OTA 状态机 |
| `uart_ota_task()` | 周期性消费 DMA 移交数据，完成头部解析、payload 接收、CRC 校验、Flash 提交和软件复位 |

**OTA 处理逻辑**:
1. `USART1_IRQHandler` 处理 IDLE 中断，`DMA0_Channel5_IRQHandler` 处理 DMA 满缓冲中断，两者都只做有界复制、置标志和 DMA 重新装载
2. **OTA 队列排空**: `uart_ota_task()` 每 5ms 最多按 `UART_OTA_TASK_DRAIN_LIMIT` 消费当前队列中的多个 DMA 移交窗口，把连续字节喂给 `uart_ota_feed_rx_bytes()`；上限默认为 `UART_OTA_RX_QUEUE_DEPTH`，防止异常输入长期占用合作式调度器
3. 状态机先拼满 64 字节 OTA 头部，校验 `magic/header_size/image_size/load_addr/header_crc32/stack_addr/entry_addr`
4. 头部合法后继续接收 payload，并在 RAM 中累计 CRC32
5. payload 完整后复核 payload CRC 和向量表，确认发送内容与头部一致
6. 校验通过后统一擦写 `0x08059000` App 缓存区，避免边接收边擦写内部 Flash 导致丢字节
7. App 回读 App 缓存区并重新计算 CRC32，确认 Flash 写入正确
8. 校验通过后调用 `bootloader_port_write_upgrade_info()` 写参数区并软件复位
9. 若用户误发 `Project.bin` 或旧格式流导致 `OTA: bad header status=...` / `OTA: payload failed status=...`，状态机会进入错误态；下一次从文件开头重新原始发送合法 `Project_ota.bin` 时，会在 magic 前缀处重新同步并继续接收，不再必须手动复位

---

### 4.7 adc_app.c/h - ADC 采集任务

**文件路径**: `Function/adc_app.c`, `Function/adc_app.h`

**功能**: 更新 ADC 采集数据到显示缓冲区。

**实现说明**:
- `adc_value[2]` 是 ADC DMA 后台更新的共享缓冲区，驱动层声明为 `__IO uint16_t adc_value[2]`，任务层读取时不会被编译器按普通内存错误缓存。
- ADC/USART/SPI DMA 初始化统一先调用 `dma_single_data_para_struct_init()`，再覆盖本模块需要的字段，避免栈上未初始化字段影响 DMA 配置。

**函数说明**:

| 函数 | 功能 |
|------|------|
| `adc_task()` | 将 DMA 缓冲区中的 ADC 值复制到 `convertarr` |

**ADC 通道说明**:
- 通道 0 (ADC_CHANNEL_10): PC0, 外部模拟输入
- 通道 1 (ADC_CHANNEL_12): PC2, 内部参考电压 (Vref)

---

### 4.8 gd30ad3344_pt100_app.c/h - GD30AD3344 PT100 应用层温度换算任务

**文件路径**: `Function/gd30ad3344_pt100_app.c`, `Function/gd30ad3344_pt100_app.h`

**功能**: 基于商业版 PT100 调理模块和 GD30AD3344 ADC，把模块 Vout 换算为电阻和温度，并通过 USART0 输出调试结果。

**默认硬件参数**:

| 参数 | 当前值 | 说明 |
|------|--------|------|
| ADC 通道 | `GD30AD3344_Channel_4` | 商业版模块 Vout 按 AIN0~GND 单端读取 |
| ADC 量程 | `GD30AD3344_PGA_4V096` | 适配商业版模块带偏置的 Vout 输入范围，并保留异常输出余量 |
| Vout 零点偏置 | `0.9617V` | 代码中为 `PT100_COMMERCIAL_OFFSET_V` |
| 电阻斜率 | `0.001957V/Ω` | 代码中为 `PT100_COMMERCIAL_RESISTANCE_SLOPE_V_PER_OHM` |
| 温度公式 | `T≈2.635*R测-263.5` | 适配当前商业版模块现场标定 |
| 温度范围 | `-50℃~150℃` | 与赛题设计范围一致，越界时返回边界并清除 `range_valid` |

**换算流程**:

```text
GD30AD3344 电压 Vout -> R测=(Vout-0.9617)/0.001957 -> T≈2.635*R测-263.5 -> -50℃~150℃ 限幅
```

**USART0 调试输出**:

```text
PT100: Vout=<模块输出电压>V signal=<扣偏置电压>V R=<电阻>ohm T=<温度>C valid=<0或1>
PT100: sample failed err=<GD30AD3344最近一次错误>
```

**PT100 串口日志节流**: 采样任务仍按 200ms 周期更新测量缓存，但 USART0 浮点调试日志通过 `PT100_DEBUG_LOG_PERIOD_MS=1000` 限制为约 1s 一次，避免长日志影响 OLED、按键和 OTA 队列消费。

**函数说明**:

| 函数 | 功能 |
|------|------|
| `gd30ad3344_pt100_app_init()` | 清空测量缓存，并设置启动后首帧 ADC 样本丢弃标志 |
| `gd30ad3344_pt100_task()` | 每 200ms 读取 GD30AD3344，按商业版模块公式更新 ADC 电压、电阻和温度缓存，并按日志节流周期通过 USART0 打印 |
| `gd30ad3344_pt100_get_latest()` | 返回最近一次 `pt100_measurement_t` 测量结果副本 |

**注意事项**:
- 若后续用实测两点重新标定商业版模块，应同步修改 `PT100_COMMERCIAL_OFFSET_V`、`PT100_COMMERCIAL_RESISTANCE_SLOPE_V_PER_OHM`、温度线性参数和本文档。
- 若后续改为差分接法，应把 `PT100_ADC_CHANNEL` 从 `GD30AD3344_Channel_4` 改为实际差分 MUX。
- 若 `GD30AD3344_AD_Read()` 返回 `-1`，本轮不会把失败哨兵值换算为温度；任务会清除 `sample_ready/range_valid`，并按节流周期打印 `PT100: sample failed err=...`。
- 当前 app 层会向 USART0 输出 PT100 调试行；如果后续不需要持续刷日志，可把 `PT100_DEBUG_LOG_PERIOD_MS` 调大，或关闭 `gd30ad3344_pt100_task()` 中受 `prv_pt100_should_log()` 保护的 `my_printf(DEBUG_USART, ...)`。

---

### 4.9 rtc_app.c/h - RTC 时间显示任务

**文件路径**: `Function/rtc_app.c`, `Function/rtc_app.h`

**功能**: 读取 RTC 当前时间并在 OLED 第 3 行显示。

**函数说明**:

| 函数 | 功能 |
|------|------|
| `rtc_task()` | 调用 `rtc_current_time_get()` 获取时间，格式化显示 `HH:MM:SS` |

---

## 5. HardWare 与 Library 目录文件详解

### 5.1 HardWare 板级支持包

**文件路径**: `HardWare/LED/bsp_led.c/h`, `HardWare/KEY/bsp_key.c/h`, `HardWare/USART/bsp_usart.c/h`, `HardWare/ANALOG/bsp_analog.c/h`, `HardWare/RTC/bsp_rtc.c/h`, `HardWare/POWER/bsp_power.c/h`

**功能**: 定义所有硬件引脚映射、外设初始化函数和全局缓冲区。

#### 5.1.1 头文件

**引脚映射定义**:

| 外设 | 引脚 | 功能 |
|------|------|------|
| LED1-LED6 | PD10-PD15 | 用户 LED (高电平点亮) |
| KEY1-KEY6 | PB1,PC5,PC4,PA7,PA6,PA5 | 用户按键 (低电平有效) |
| KEYW | PA0 | 唤醒按键 |
| OLED SCL | PB8 | I2C0 时钟 |
| OLED SDA | PB9 | I2C0 数据 |
| SPI Flash SCK | PB3 | SPI0 时钟 |
| SPI Flash MISO | PB4 | SPI0 主入从出 |
| SPI Flash MOSI | PB5 | SPI0 主出从入 |
| SPI Flash CS | PA15 | SPI0 片选 |
| GD30AD3344 SCK | PE12 | SPI3 时钟 |
| GD30AD3344 MISO | PE13 | SPI3 主入从出 |
| GD30AD3344 MOSI | PE14 | SPI3 主出从入 |
| GD30AD3344 CS | PE10 | SPI3 片选 |
| USART0 TX/RX | PA9/PA10 | 调试串口 / SMARTFS 调试命令入口 |
| USART1 TX/RX | PD5/PD6 | RS485/OTA 专用串口 |
| RS485 DIR | PE8 | MAX3485 DE/RE# 方向控制，高电平发送、低电平接收 |
| USART5 TX/RX | PC6/PC7 | 预留串口 |
| ADC IN10 | PC0 | 外部模拟输入 |
| ADC IN12 | PC2 | 内部参考电压 |
| DAC OUT | PA4 | DAC 模拟输出 |

**LED 控制宏**:

| 宏 | 功能 |
|----|------|
| `LED1_SET(x)` ~ `LED6_SET(x)` | 设置 LED 状态 (0=灭, 非0=亮) |
| `LED1_TOGGLE` ~ `LED6_TOGGLE` | 翻转 LED 状态 |
| `LED1_ON` ~ `LED6_ON` | 点亮 LED |
| `LED1_OFF` ~ `LED6_OFF` | 熄灭 LED |

**按键读取宏**:

| 宏 | 功能 |
|----|------|
| `KEY1_READ` ~ `KEY6_READ` | 读取按键状态 (0=按下, 1=释放) |
| `KEYW_READ` | 读取唤醒键状态 |

**DMA 通道分配**:

| 外设 | DMA 控制器 | 通道 | 子外设 |
|------|-----------|------|--------|
| USART0 RX | DMA1 | CH5 | SUBPERI4 |
| USART1 RX | DMA0 | CH5 | SUBPERI4 |
| USART5 RX | DMA1 | CH1 | SUBPERI5 |
| ADC0 | DMA1 | CH0 | SUBPERI0 |
| I2C0 TX | DMA0 | CH6 | SUBPERI1 |
| SPI Flash RX | DMA1 | CH2 | SUBPERI3 |
| SPI Flash TX | DMA1 | CH3 | SUBPERI3 |

#### 5.1.2 源文件

**全局缓冲区**:

| 变量 | 大小 | 用途 |
|------|------|------|
| `oled_cmd_buf[2]` | 2B | OLED 命令缓冲区 |
| `oled_data_buf[129]` | 129B | OLED 数据 DMA 缓冲区，1B 控制字 + 最多 128B 页数据 |
| `spi3_send_array[12]` | 12B | SPI3 DMA 发送缓冲区 |
| `spi3_receive_array[12]` | 12B | SPI3 DMA 接收缓冲区 |
| `spi1_send_array[12]` | 12B | SPI0 DMA 发送缓冲区 |
| `spi1_receive_array[12]` | 12B | SPI0 DMA 接收缓冲区 |
| `usart0_rxbuffer[1024]` | 1024B | USART0 DMA 接收缓冲区，用于 SMARTFS 文本命令帧 |
| `usart1_rxbuffer[1024]` | 1024B | USART1/RS485 DMA 裸流接收窗口，用于连续发送 `Project_ota.bin` 时按 IDLE 或 DMA 满缓冲分段移交 |
| `usart5_rxbuffer[256]` | 256B | USART5 DMA 接收缓冲区 |
| `adc_value[2]` | 4B | ADC 采样值 (2 通道)，ADC DMA 缓冲区为 __IO |
| `convertarr[1]` | 2B | DAC 输出缓冲区 |
| `rtc_initpara` | - | RTC 初始化参数结构体 |

**BSP 初始化函数**:

| 函数 | 功能 |
|------|------|
| `bsp_led_init()` | 配置 PD10-PD15 为推挽输出，初始熄灭 |
| `bsp_btn_init()` | 配置按键引脚为上拉输入 |
| `bsp_oled_init()` | 配置 I2C0 (400kHz) + DMA CH6 |
| `bsp_gd25qxx_init()` | 配置 SPI0 (主模式, 全双工, 8分频) |
| `bsp_gd30ad3344_init()` | 配置 SPI3 (主模式, 全双工, 8分频) |
| `bsp_usart0_init()` | 配置 USART0 (115200, DMA 接收, IDLE 中断) |
| `bsp_usart1_init()` | 配置 USART1/RS485 (115200, DMA 接收, IDLE 中断, OTA 专用) |
| `bsp_usart5_init()` | 配置 USART5 (115200, DMA 接收) |
| `bsp_usart_all_init()` | 初始化 USART0、USART1/RS485 和 USART5 |
| `bsp_adc_init()` | 配置 ADC0 (连续扫描, 2 通道, DMA) |
| `bsp_dac_init()` | 配置 DAC0 (TIMER5 触发, PA4 输出) |
| `bsp_rtc_init()` | 配置 RTC；首选外部 32.768kHz 晶振，冷启动 LXTAL 失败时可回退 IRC32K；VBAT 有效时恢复备份域时间 |

**低功耗相关函数**:

| 函数 | 功能 |
|------|------|
| `bsp_enter_sleep()` | 进入 Sleep 轻量睡眠模式，暂停 CPU 和 SysTick，KEYW/EXTI0 唤醒后继续运行 |
| `bsp_enter_deepsleep()` | 进入 Deep-sleep 深度睡眠模式，关闭当前不需要的外设和时钟，KEYW/EXTI0 唤醒后重建资源 |
| `bsp_enter_standby()` | 进入 Standby 待机模式，启用 KEYW/PA0 的 PMU WKUP 唤醒脚，唤醒后按复位流程重新启动 |
| `bsp_wkup_key_exti_init()` | 配置唤醒键 EXTI 中断，用于 Sleep/Deep-sleep 的下降沿唤醒 |
| `bsp_wkup_key_exti_deinit()` | 唤醒恢复完成后关闭 EXTI0，避免低功耗专用中断残留 |
| `bsp_usart_disable_for_deepsleep()` | 睡眠前关闭所有串口 |
| `bsp_oled_disable_for_deepsleep()` | 睡眠前关闭 OLED |
| `bsp_spi_disable_for_deepsleep()` | 睡眠前让 SPI Flash / GD30AD3344 进入芯片级待机，并关闭所有 SPI |
| `bsp_clock_disable_for_deepsleep()` | 睡眠前统一关闭不再需要的外设时钟门控 |
| `bsp_gpio_enter_deepsleep_state()` | 配置所有 GPIO 为模拟输入 (最低功耗) |
| `bsp_deepsleep_reinit_after_wakeup()` | 唤醒后重新初始化所有外设 |

**三档低功耗按键映射**:

| 按键 | 模式 | 睡前动作 | 唤醒方式 | 唤醒后行为 |
|------|------|----------|----------|------------|
| KEY1 | Sleep | LED1 翻转，配置 KEYW/EXTI0，停止 SysTick | 按 KEYW，PA0 下降沿触发 EXTI0 | 函数返回，恢复 timebase 和调度器基线 |
| KEY2 | Deep-sleep | LED2 翻转，关闭外设/总线/GPIO/时钟，配置 KEYW/EXTI0 | 按 KEYW，PA0 下降沿触发 EXTI0 | 函数返回，重建时钟、VTOR、timebase 和外设 |
| KEY3 | Standby | 不再翻转 LED3；先关闭 OLED 和全部 LED，再等待 KEY4 按下后松开确认，随后关闭外设并启用 PMU WKUP | 按 KEYW/PA0 触发 PMU WKUP | 芯片按复位流程重启，串口输出 `BOOT: wake from standby` |

**Sleep 流程**:
1. 读取 `bsp_rtc_get_epoch_seconds()` 保存睡前秒级时间戳
2. 临时屏蔽 USART0 和 USART1 的 NVIC 中断，避免 Sleep 被运行态外设旧中断立即唤醒
3. 配置 KEYW/PA0 的 `EXTI0` 下降沿唤醒
4. 调用 `timebase_prepare_reconfiguration()` 停止 SysTick，避免 1ms tick 立即唤醒 CPU
5. 调用 `pmu_to_sleepmode(WFI_CMD)` 进入 Sleep
6. KEYW 唤醒后调用 `timebase_update_after_clock_change()` 恢复本地 timebase
7. 用 RTC 秒差执行 RTC 补偿，恢复被临时屏蔽的运行态外设中断，并调用 `scheduler_reset_runtime()` 重置调度器运行基线
8. 关闭 `EXTI0` 低功耗唤醒通道，恢复普通按键扫描

**深度睡眠流程**:
1. 关闭所有串口 DMA 和中断
2. 关闭 OLED 电荷泵和显示输出，再关闭 I2C
3. 让 GD25QXX 进入 deep power-down、让 GD30AD3344 切到掉电/单次转换模式，再关闭 SPI 总线
4. 关闭 ADC、DAC、定时器
5. 将所有 GPIO 配置为模拟输入 (最低漏电)
6. 配置唤醒键 EXTI 中断
7. 关闭不再需要的 USART / SPI / I2C / DMA / ADC / DAC / TIMER / GPIO 时钟门控
8. 清除 PMU 标志
9. 读取 `bsp_rtc_get_epoch_seconds()` 保存睡前秒级时间戳，供唤醒后 RTC 补偿 timebase
10. 调用 `timebase_prepare_reconfiguration()` 完整停止 SysTick 计数器和中断
11. 调用 `pmu_to_deepsleepmode(PMU_LDO_LOWPOWER, PMU_LOWDRIVER_ENABLE, WFI_CMD)` 进入深度睡眠
12. 唤醒后调用 `timebase_update_after_clock_change()` 恢复本地 timebase，再用醒后 RTC 秒差执行 RTC 补偿
13. 重新初始化必须立即恢复的外设，并在 SPI 总线恢复后释放 GD25QXX 的 deep power-down
14. 调用 `scheduler_reset_runtime()` 执行调度器唤醒重基线，避免周期任务在恢复后第一轮集中执行
15. 初始化完成后关闭 `EXTI0` 唤醒中断，只保留普通按键输入功能

**Standby 流程**:
1. KEY3 按下后先发送 OLED 关显示/关电荷泵命令，并关闭全部 LED，让确认等待阶段进入暗屏暗灯状态
2. 等待用户按下 KEY4 后松开，KEY4 松开高电平稳定 20ms 后作为进入 Standby 的确认
3. 关闭所有串口 DMA 和中断
4. 关闭 OLED 总线、SPI、ADC、DAC、TIMER 等外设
5. 将 GPIO 收拢为低漏电状态，保留 KEYW/PA0 作为 PMU WKUP 唤醒脚，KEY4 在进入真正 Standby 前保持输入用于确认
6. 清除 `PMU_FLAG_WAKEUP` 和 `PMU_FLAG_STANDBY`
7. 调用 `pmu_wakeup_pin_enable()` 启用 PMU WKUP
8. 调用 `timebase_prepare_reconfiguration()` 停止 SysTick
9. 调用 `pmu_to_standbymode()` 进入 Standby
10. 按 KEYW/PA0 触发 PMU WKUP 后，芯片从复位入口重新启动
11. 启动阶段打开 PMU 时钟并检测 `PMU_FLAG_STANDBY`，调试串口输出 `BOOT: wake from standby` 后清除 PMU Standby/Wakeup 标志

**RTC / VBAT 行为说明**:
- 当前板上 `RTC` 使用外部 `32.768kHz` 晶振。
- `RTC_CLOCK_FALLBACK_IRC32K_ENABLE=1` 时，冷启动且备份域未选择时钟源的情况下，如果 LXTAL 启动失败，会回退到片内 IRC32K，并使用 `prescaler_s=0x13F`、`prescaler_a=0x63`。
- 若备份域已经保存 RTCSRC，且 RTCSRC 为 LXTAL，则 `bsp_rtc_init()` 不会强制改写时钟源；这可以保护 VBAT 供电期间已经运行的 RTC。
- 若备份域保存的 RTCSRC 为 IRC32K，`bsp_rtc_init()` 会先尝试启动外部 LXTAL。若 LXTAL 稳定，会在复位备份域前读取当前 RTC 时间快照，随后清除旧 RTCSRC、切回 LXTAL、使用 `prescaler_s=0xFF` 和 `prescaler_a=0x7F` 重新写回时间，并把 `rtcstat` 的 `recovered` 字段置为 `1`。
- 若 LXTAL 仍不可用，固件不会复位备份域，会继续沿用 IRC32K 和已有 RTC 时间，避免在现场丢失当前时间；此时 `rtcstat` 会显示 `src=IRC32K`。
- 若 `VBAT` 端存在纽扣电池供电，且备份域标记 `RTC_BKP0 == BKP_VALUE` 仍有效，则主电掉电后再次上电会继续沿用原来的 RTC 时间。
- 若 `VBAT` 未供电，或备份域从未初始化/已丢失，则 `bsp_rtc_init()` 会按工程默认时间重新建表。
- 现场发现 RTC 长期走时明显偏快或偏慢时，先通过 USART0 执行 `rtcstat`。`src=LXTAL` 表示正在使用外部 32.768kHz 晶振；`src=IRC32K` 表示正在使用片内 RC，长期误差会明显变大，应检查外部晶振和负载电容并重新上电确认是否能迁回 LXTAL。

---

### 5.2 OLED - SSD1306 OLED 驱动

**文件路径**: `HardWare/OLED/bsp_oled.c`, `HardWare/OLED/bsp_oled.h`, `HardWare/OLED/oled.c`, `HardWare/OLED/oled.h`

**功能**: 驱动 0.91 英寸 SSD1306 OLED 显示屏 (128x32, I2C 接口)。

**主要函数**:

| 函数 | 功能 |
|------|------|
| `OLED_Init()` | 初始化 SSD1306 (发送初始化命令序列) |
| `OLED_Clear()` | 清屏 (填充 0x00) |
| `OLED_Display_On()` | 开启显示 |
| `OLED_Display_Off()` | 关闭电荷泵并发送 `0xAE` 关显示，用于深睡前真正息屏 |
| `OLED_Set_Position(x, y)` | 设置显示位置 |
| `OLED_ShowChar(x, y, ch, fontsize)` | 显示单个字符 |
| `OLED_ShowStr(x, y, ch, fontsize)` | 显示字符串 |
| `OLED_ShowNum(x, y, num, length, fontsize)` | 显示数字 |
| `OLED_ShowFloat(x, y, num, accuracy, fontsize)` | 显示浮点数 |
| `OLED_ShowHanzi(x, y, no)` | 显示汉字 (16x16) |
| `OLED_ShowPic(x0, y0, x1, y1, BMP)` | 显示图片 |
| `OLED_Allfill()` | 全屏填充 |
| `OLED_Write_cmd(cmd)` | 写命令 (通过 I2C DMA) |
| `OLED_Write_cmd_buf(cmds, length)` | 批量写 OLED 命令，按 128B 命令块自动分块发送 |
| `OLED_Write_data(data)` | 写数据 (通过 I2C DMA) |
| `OLED_Write_data_buf(data, length)` | 批量写显存数据，按 128B 数据块自动分块发送 |

**I2C 通信协议**:
- 设备地址: 0x78 (7 位地址 0x3C 左移 1 位)
- 命令格式: `[控制字节 0x00] [命令字节]`
- 数据格式: `[控制字节 0x40] [数据字节]`
- 使用 DMA CH6 传输，提高效率

**OLED 底层批量写入优化**:
- `OLED_Write_cmd()` 和 `OLED_Write_data()` 复用统一的 `oled_write_packet()` I2C/DMA 事务 helper，集中处理 START、地址发送、DMA 完成、BTC、STOP 和异常恢复。
- `OLED_Write_cmd_buf()`、`OLED_Write_data_buf()`、`OLED_Set_Position()` 和 `OLED_ShowStr()` 会返回 1/0 表示底层事务是否成功，上层可据此避免缓存假成功。
- `oled_data_buf` 扩展为 `OLED_TX_BUFFER_SIZE=129` 字节，首字节是 SSD1306 数据控制字 `0x40`，后续最多携带一整页 128 字节显存数据。
- `OLED_Write_data_buf()` 会按 `OLED_TX_DATA_MAX_SIZE=128` 自动分块，因此旧接口可以继续写单字节，新路径可以批量写字符、图片和整页。
- `OLED_Clear()` 和 `OLED_Allfill()` 每页只发送 1 次批量数据事务；清屏数据事务从 512 次单字节发送降为 4 次页级发送。
- `OLED_ShowChar()`、`OLED_ShowHanzi()`、`OLED_ShowHzbig()` 和 `OLED_ShowPic()` 按字模或页段批量写入，减少每次显示刷新中的 START/STOP 数量。

**OLED 二轮事务压缩优化**:
- `OLED_Write_cmd_buf()` 支持连续命令批量发送，`OLED_Write_cmd()` 保留为单命令兼容封装。
- `OLED_Set_Position()` 通过 `oled_set_position_buf()` 一次发送页地址、高列地址和低列地址，定位从 3 次 I2C 命令事务压缩为 1 次。
- `OLED_ShowStr()` 不再逐字符调用 `OLED_ShowChar()`；6x8 字符按 8 像素步进拼入行缓冲，每个字符保留 2 列空白间距，16 个字符最多一次 128B 数据事务完成一行刷新。
- 8x16 字符串按上、下两页分别拼接并批量写入，避免每个字符重复定位。

**I2C 总线恢复机制**:
- `I2C_Bus_Reset()`: 当 I2C 总线锁死时，手动产生 9 个 SCL 脉冲释放 SDA
- 超时检测: `oled_wait_i2c_flag_set()` 防止死循环；总线忙等待不再使用 10000ms 级长阻塞，异常 OLED 会快速返回失败并把 `s_oled_available` 置为 0，避免卡死合作式调度器

**OLED 应用层刷新优化**:
- `oled_printf()` 使用 `OLED_APP_LINE_BUFFER_SIZE=32` 的行级栈缓冲，不再使用 512 字节临时缓冲。
- `g_oled_line_cache` 记录 4 行上一次显示文本，`oled_printf()` 仅刷新变化字符段；例如 `uwTick` 或 ADC 文本只变末尾几位时，不再整行重写 16 个字符。
- 每行按 128 像素宽度裁剪到 16 个 8 像素字符，并补空格覆盖旧长字符串残留。
- **OLED 失败不上缓存**: `oled_printf()` 只有在 `OLED_ShowStr()` 返回成功后才更新行缓存；若 I2C/DMA 失败，缓存保持旧值，下一轮会继续尝试写入。
- `OLED_Init()` 清屏后必须调用 `oled_app_reset_cache()`，唤醒恢复也会重置缓存，避免物理屏幕已清空但应用层误判文本未变化。

---

### 5.3 GD25QXX - SPI Flash 驱动

**文件路径**: `HardWare/GD25QXX/gd25qxx.c`, `HardWare/GD25QXX/gd25qxx.h`, `HardWare/GD25QXX/smartfs_port.c`, `HardWare/GD25QXX/smartfs_port.h`

**功能**: 驱动 GD25Qxx 系列 SPI Flash 存储器。

**主要函数**:

| 函数 | 功能 |
|------|------|
| `spi_flash_init()` | 读取 Flash ID 并初始化 |
| `spi_flash_read_id()` | 读取 JEDEC ID (厂商 + 容量) |
| `spi_flash_sector_erase(sector_addr)` | 扇区擦除 (4KB)，成功返回 `0`，命令阶段 DMA 或 WIP 等待失败返回 `-1` |
| `spi_flash_bulk_erase()` | 整片擦除，成功返回 `0`，失败返回 `-1` |
| `spi_flash_page_write(pbuffer, write_addr, num)` | 页编程 (256B)，成功返回 `0`，参数非法、命令阶段 DMA 或 WIP 等待失败返回 `-1` |
| `spi_flash_buffer_write(pbuffer, write_addr, num)` | 自动跨页写入，所有分页写入成功返回 `0`，任一页失败返回 `-1` |
| `spi_flash_buffer_read(pbuffer, read_addr, num)` | 读取数据 |
| `spi_flash_write_enable()` | 写使能，成功返回 `0`，WREN 命令 DMA 失败返回 `-1` |
| `spi_flash_wait_for_write_end()` | 等待写操作完成，带超时返回，避免 Flash 异常时永久阻塞 |
| `spi_flash_enter_deep_power_down()` | 发送 `0xB9`，让 Flash 进入芯片级深掉电；失败返回 `-1` |
| `spi_flash_release_from_deep_power_down()` | 发送 `0xAB`，让 Flash 从深掉电恢复；失败返回 `-1` |
| `spi_flash_send_byte_dma(byte)` | DMA 方式发送单字节 |
| `spi_flash_transmit_receive_dma(tx, rx, size)` | DMA 方式收发数据 |
| `smart_storage_init()` | 检查 GD25QXX SMARTFS 元数据；必要时格式化整片文件系统 |
| `smart_storage_self_test()` | 初始化 SMARTFS，并执行目录创建、文件写入/读回、目录大小和递归删除校验 |
| `test_spi_flash()` | 裸 Flash 读写测试；默认关闭；启用后会擦写 0x000000 并破坏 SMARTFS |

**SPI 配置**:
- 模式: 主机, 全双工
- 时钟极性/相位: CPOL=1, CPHA=1 (模式 3)
- 分频: 8 分频
- 数据位序: MSB 先行
- DMA: `DMA1 CH2` 接收、`DMA1 CH3` 发送、`SUBPERI3`，初始化时必须打开 `RCU_DMA1`
- 低功耗策略: 深度睡眠前发送 `deep power-down` 指令，唤醒后在 SPI0 恢复完成后发送 `release` 指令；WIP 等待和 DMA FTF 等待均带超时，低功耗入口根据返回值继续收拢总线，避免 USART 已关闭后卡死在休眠准备阶段

**SMARTFS 分区与安全策略**:

| 项目 | 当前值 | 说明 |
|------|------|------|
| 物理 Flash 容量 | `2MB` | `SMARTFS_FLASH_TOTAL_SIZE`，按 GD25Q16 配置 |
| SMARTFS 管理区 | `0x000000` ~ `0x1FFFFF` | `SMARTFS_FLASH_FS_SIZE`，整片 512 个 4KB 块 |
| 元数据区 | `0x000000` ~ `0x007FFF` | 两个 16KB 元数据副本，轮换提交 |
| 数据区 | `0x008000` ~ `0x1FFFFF` | 文件数据块池 |
| 裸测试保留区 | 无 | 用户已取消末尾 4KB 保留区，整片都归 SMARTFS |
| SMARTFS 自检 | 默认启用 | `SMART_STORAGE_BOOT_SELF_TEST_ENABLE=1` |
| 裸 Flash 测试 | 默认关闭 | `SPI_FLASH_RAW_TEST_ENABLE=0` |

启动时默认运行 `smart_storage_self_test()`。该测试会检查 SMARTFS 元数据；如果首次使用、旧 LittleFS 数据仍在或元数据无效，则格式化整片 2MB GD25Q16。加载元数据时会校验目录项父子关系、同目录重名、父目录环和文件块链长度；提交或旧块清理失败后会清除加载标记，下一条命令重新从 Flash 读取一致状态。自检会创建 `/selftest/hello.txt`，读回校验内容，验证目录大小后删除测试目录。`test_spi_flash()` 只保留给底层 GD25QXX 驱动排障；因为当前没有裸测保留区，启用后会擦写 `0x000000` 起始扇区并破坏 SMARTFS。

GD25QXX 写/擦 API 会把 `WREN` 写使能、命令/地址/数据字节 DMA 失败和 WIP 等待超时都上抛为 `-1`。SMARTFS 在释放块链、写数据链、追加文件、提交元数据副本和全盘格式化时会检查这些返回码；底层失败统一映射为 `SMART_STORAGE_ERR_IO`，避免在 Flash 实际未写入时继续提交错误的 RAM 元数据。

**预期串口输出**:

首次空片、旧 LittleFS 数据或 SMARTFS 元数据损坏时，预期会看到格式化日志：

```text
SMARTFS: metadata invalid, format whole flash
SMARTFS: self-test PASS: /selftest/hello.txt=SMARTFS self-test OK
BOOT: test_spi_flash skipped (SPI_FLASH_RAW_TEST_ENABLE=0)
```

后续启动元数据有效时，预期直接完成自检：

```text
SMARTFS: self-test PASS: /selftest/hello.txt=SMARTFS self-test OK
BOOT: test_spi_flash skipped (SPI_FLASH_RAW_TEST_ENABLE=0)
```

---

### 5.4 GD30AD3344 - SPI ADC 驱动

**文件路径**: `HardWare/GD30AD3344/gd30ad3344.c`, `HardWare/GD30AD3344/gd30ad3344.h`

**功能**: 驱动 GD30AD3344 16 位 SPI ADC 芯片。

**寄存器位域定义**:

| 位域 | 位 | 功能 |
|------|-----|------|
| SS | [15] | 启动转换 (写 1 开始单次转换) |
| MUX | [14:12] | 输入多路选择 (8 种组合) |
| PGA | [11:9] | 可编程增益放大器 (±6.144V ~ ±0.256V) |
| MODE | [8] | 工作模式 (0=连续, 1=单次) |
| DR | [7:5] | 数据速率 (6.25 ~ 1000 SPS) |
| PULL_UP_EN | [3] | DOUT 上拉电阻使能 |
| NOP | [2:1] | 空操作控制 |

**输入通道选择**:

| MUX 值 | 差分输入 | 单端输入 |
|--------|----------|----------|
| 0 | AIN0 - AIN1 | - |
| 1 | AIN0 - AIN3 | - |
| 2 | AIN1 - AIN3 | - |
| 3 | AIN2 - AIN3 | - |
| 4 | - | AIN0 - GND |
| 5 | - | AIN1 - GND |
| 6 | - | AIN2 - GND |
| 7 | - | AIN3 - GND |

**主要函数**:

| 函数 | 功能 |
|------|------|
| `GD30AD3344_Init()` | 初始化 ADC 配置寄存器 |
| `GD30AD3344_Enter_LowPower()` | 切到 `MODE=1` 掉电/单次转换配置，用于深睡前降低芯片功耗；失败返回 `-1` |
| `GD30AD3344_Exit_LowPower()` | 恢复到工程默认运行态配置；失败返回 `-1` |
| `GD30AD3344_AD_Read(CH, Ref, &out_voltage_v)` | 读取指定通道并输出电压 (V)，成功返回 `0`，输出指针为空或 SPI DMA 失败返回 `-1` |
| `GD30AD3344_GetLastError()` | 查询最近一次 GD30AD3344 SPI/DMA 操作错误，`0` 表示无错误，非 `0` 表示最近一次采样或配置失败 |

**低功耗说明**:
- 当前板级没有给 `GD30AD3344` 提供独立电源开关，因此深度睡眠前通过配置寄存器把 `MODE` 切到 `1`，让芯片进入掉电/单次转换模式。
- 唤醒后重新执行 `GD30AD3344_Init()`，恢复连续转换运行态。
- GD30AD3344 SPI DMA 等待带超时保护；若低功耗配置下发失败，电源入口仍会关闭 SPI/DMA/时钟，避免一个外置 ADC 异常阻断深睡流程。
- 应用层必须检查 `GD30AD3344_AD_Read()` 返回值，失败时不能把 `0xFFFF` 等同步回读哨兵值换算为电压或温度。
- PGA 量程映射按枚举显式处理：`GD30AD3344_PGA_0V256` 对应 `0.256V`，`GD30AD3344_PGA_0V064` 对应 `0.064V`，未知枚举按最保守 `0.064V` 返回。

---

## 6. 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      main() 主循环                          │
│                    while(1) scheduler_run()                 │
├─────────────────────────────────────────────────────────────┤
│                      调度器 (scheduler)                      │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┬─────────────┐ │
│  │ led_task │ adc_task │oled_task │ btn_task │ uart_task│ uart_ota_task│ │
│  │   1ms    │  100ms   │  100ms   │   5ms    │   5ms    │     5ms      │ │
│  └────┬─────┴────┬─────┴────┬─────┴────┬─────┴────┬─────┴──────┬──────┘ │
├───────┼──────────┼──────────┼──────────┼──────────┼────────────┼─────────┤
│       │          │          │          │          │            │         │
│  ┌────▼────┐ ┌───▼───┐ ┌───▼───┐ ┌───▼───┐ ┌───▼────┐   ┌──────▼─────┐  │
│  │LED GPIO │ │ ADC0  │ │OLED   │ │按键扫描│ │USART0  │   │RS485 OTA   │  │
│  │PD10-PD15│ │DMA CH0│ │I2C0   │ │ 5ms任务 │ │ RS485  │   │   DMA CH1  │  │
│  └─────────┘ └───────┘ │DMA CH6│ └───────┘ └────────┘   └────────────┘  │
│                         └───────┘                                         │
├─────────────────────────────────────────────────────────────┤
│                      BSP 层 (mcu_cmic)                      │
│  ┌─────────┬─────────┬─────────┬─────────┐                │
│  │ SPI0    │ SPI3    │ DAC0    │  RTC    │                │
│  │Flash    │ADC      │ TIMER5  │ 32kHz   │                │
│  └─────────┴─────────┴─────────┴─────────┘                │
├─────────────────────────────────────────────────────────────┤
│                 GD32F4xx 标准外设库                          │
├─────────────────────────────────────────────────────────────┤
│                 GD32F470VE 硬件                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 编译配置

### 7.1 预定义宏

```
USE_STDPERIPH_DRIVER
GD32F470
```

### 7.2 头文件包含路径

```
..\Library\GD32F4xx_standard_peripheral\Include
..\User
..\HeaderFiles
..\Function
..\HardWare\LED
..\HardWare\KEY
..\HardWare\USART
..\HardWare\OLED
..\HardWare\STORAGE
..\HardWare\ANALOG
..\HardWare\RTC
..\HardWare\POWER
..\HardWare\BOOTLOADER
..\HardWare\GD25QXX
..\HardWare\GD30AD3344
..\CMSIS
..\CMSIS\GD\GD32F4xx\Include
```

### 7.3 内存配置

| 区域 | 起始地址 | 大小 |
|------|----------|------|
| IROM (Flash / App 区) | 0x0800D000 | 0x26000 |
| IRAM (主 SRAM) | 0x20000000 | 192KB |
| IRAM2 (CCM SRAM) | 0x10000000 | 64KB |

说明：当前工程作为 BootLoader App 使用，不能再链接到 `0x08000000`。`0x08000000` 必须留给 BootLoader。

---

## 8. 使用说明

### 8.1 编译

1. 打开 `project/2026706296.uvprojx`
2. 执行 `Project -> Rebuild all target files`
3. 确保安装了 `GigaDevice.GD32F4xx_DFP.3.0.3` Pack
4. 当前工程已打开 HEX 输出，并在构建后调用 `fromelf` 生成 `project/output/Project.bin`，随后调用 `tools/pack_ota_image.exe` 生成 `project/output/Project_ota.bin`

| 输出文件 | 用途 |
|------|------|
| `project/output/Project.hex` | 带地址信息，可用于烧录工具直接写入 App 区 `0x0800D000` |
| `project/output/Project.bin` | 纯二进制 App payload，由 Keil After Build 作为打包输入，不作为现场 OTA 文件发送 |
| `project/output/Project_ota.bin` | 64 字节 OTA 头部 + `Project.bin` payload，现场通过 RS485/USART1 原始/直接发送 |

### 8.1.1 AC6 脱机运行检查

当前工程使用 ARMCLANG / AC6 编译，App 必须禁用 C 库 semihosting，并由 `User/main.c` 提供 `_sys_open()`、`_sys_write()`、`_sys_exit()`、`_ttywrch()` 和 `fputc()` 等 retarget 桩函数。

| 检查项 | 正确状态 | 作用 |
|---|---|---|
| `__use_no_semihosting` | `project/Listings/Project.map` 中应出现该符号 | 告诉 Arm C 库不要通过调试器执行主机文件/控制台服务 |
| `_sys_open` / `_sys_write` / `_sys_exit` / `_ttywrch` | 符号来源应为 `main.o` | 避免链接到 C 库默认 semihosting 桩函数 |
| 调试器反汇编 | 不应停在 `BKPT 0xAB` | `BKPT 0xAB` 是 semihosting 请求，脱机运行时通常会 HardFault |
| 脱机串口日志 | BootLoader `jump app` 后应继续出现 `BOOT: handoff start` | 证明 App 已经进入自己的初始化流程 |

如果串口只打印到 `BootLoader : jump app vtor:0x0800d000 ...`，但 Keil 调试模式点继续几次后能运行，优先检查 App 是否重新引入了 semihosting，而不要先修改 BootLoader 跳转地址或厂家 `system_gd32f4xx.c`。

### 8.2 下载

1. 首次调试时，先将官方 BootLoader 烧录到 `0x08000000`
2. 当前工程编译出的 App 镜像链接地址为 `0x0800D000`
3. 通过 BootLoader 下载/搬运 App 时，App 必须写入 `0x0800D000`
4. 若临时使用 SWD 直接下载当前工程，需要确认不会擦除 `0x08000000` 的 BootLoader 区

注意：当前 BootLoader 已迁移到 `D:\GD32\2026706296_bootloader`，并已针对本 App 做过优化，不再使用官方原始的 `CONFIG_APP_SIZE 1024*20` 整包缓存，也不再固定只擦除 App 开头 `3 * 4KB`。现在 BootLoader 使用三分区 OTA：先以 1024B 分块把 `0x0800D000` 运行区完整备份到 `0x08033000`，再按 `appSize` 从 `0x08059000` 缓存区搬运新 App 到 `0x0800D000`，写后重新做 CRC 校验；若新 App 搬运失败，会尽量从备份区恢复旧 App。当前 App 已实现 `RS485/USART1` 头部 BIN 裸流在线升级：现场只发送 `project/output/Project_ota.bin`，发送格式仍是 64 字节头部 + 原始 `Project.bin` payload，一次性原始/直接发送完整文件。App 校验头部和 payload 后写缓存区，再写参数区并复位；BootLoader 随后执行备份、搬运和 CRC 校验。`USART0` 仅保留日志和 SMARTFS 调试命令。若板子里仍是旧 BootLoader，请同步重刷新的 BootLoader，否则实际升级仍会被旧容量或搬运逻辑限制。

### 8.2.1 RS485/USART1 在线升级

推荐现场使用串口工具的原始/直接发送文件功能：

| 步骤 | 操作 | 预期现象 |
|---:|---|---|
| 1 | 在 Keil 中重新编译当前 App 工程 | 生成 `project/output/Project.bin` 和 `project/output/Project_ota.bin` |
| 2 | 打开 RS485/USART1 对应串口 | 波特率设置为 `115200`，8N1 |
| 3 | 重新上电或复位板子 | RS485 口在启动自检和调度器初始化完成后看到一次 `OTA485: ready, send Project_ota.bin raw`，看到后即可发送 |
| 4 | 在串口工具中选择原始/直接发送文件 | 选择 `project/output/Project_ota.bin` |
| 5 | 等待发送完成 | `USART0` 日志出现 `OTA: header ok` 和 `OTA: payload ok` |
| 6 | 等待 App 自动复位 | `USART0` 日志出现 `OTA: ready, reset to BootLoader size=... version=...` |
| 7 | 观察 BootLoader 日志 | 出现 `app crc32 check pass` 和 `app update success` |

`Project_ota.bin` 头部格式如下：

| 偏移 | 字段 | 说明 |
|---:|---|---|
| `0x00` | `magic = 0x474F5441` | 判断是否为当前 OTA 文件 |
| `0x04` | `header_size = 64` | 固定头部长度 |
| `0x08` | `image_size` | App payload 字节数 |
| `0x0C` | `load_addr = 0x0800D000` | App 正式写入地址 |
| `0x10` | `version` | 写入 BootLoader 参数区的版本号 |
| `0x14` | `image_crc32` | App payload CRC32 |
| `0x18` | `flags = 0` | 预留 |
| `0x1C` | `header_crc32` | 头部 CRC32，计算时本字段置 0 |
| `0x20` | `stack_addr` | App 向量表第 0 项 MSP |
| `0x24` | `entry_addr` | App 向量表第 1 项 Reset_Handler |

限制：当前 App 运行区、备份区、缓存区各为 `152KB`，因此 `Project_ota.bin` 中的 App payload，也就是原始 `Project.bin`，不能超过 `152KB`。如果后续 App 超过 `152KB`，需要继续重新规划 Flash 分区或改为外部 SPI Flash 缓存升级。

### 8.3 串口调试

1. 连接 USART0 (PA9-TX, PA10-RX)
2. 波特率: 115200, 8N1
3. 上电后会打印启动日志
4. `RS485/USART1` 为 OTA 专用口，升级时请把上位机接到 RS485 总线，并确认 A/B 极性、共地和转换器方向模式正确
5. `BOOT: start` 只会出现在 `USART0`；`RS485/USART1` 上电后只会额外输出一次 `OTA485: ready`，该提示表示启动自检和调度器初始化已完成，不只是早期接线探测串
6. `USART0` 支持类 Linux 的 SMARTFS 调试命令：`help`、`pwd`、`ls`、`cd`、`cat`、`write`、`mkdir`、`touch`、`rm`、`stat`、`df`

SMARTFS 调试口示例：

```text
> help
SMARTFS SHELL:
help
gettime
rtcstat
settime <yyyy-mm-dd> <hh:mm:ss>
pwd
ls [path]
cd <path>
cat <file>
write <file> <text>
write -a <file> <text>
mkdir <dir>
touch <file>
rm <path>
stat [path]
df

> gettime
RTC: 2026-05-15 13:20:30

> rtcstat
RTC: STAT src=LXTAL ready=1 bkp=1 recovered=0 psc_a=127 psc_s=255 bdctl=0x00008103 hrfc=0x00000000 cosc=0x00000000

> settime 2026-05-16 08:00:00
RTC: SET OK 2026-05-16 08:00:00

> pwd
/

> mkdir /log
SMARTFS: MKDIR OK path=/log

> cd /log
SMARTFS: CWD /log

> touch note.txt
SMARTFS: TOUCH OK path=/log/note.txt

> write note.txt hello_sfs
SMARTFS: WRITE OK mode=overwrite len=9 path=/log/note.txt
hello_sfs

> write -a note.txt _tail
SMARTFS: WRITE OK mode=append len=14 path=/log/note.txt
hello_sfs_tail

> ls
SMARTFS: LS /log
file    14      note.txt
SMARTFS: LS done count=1

> cat note.txt
SMARTFS: CAT OK len=14 path=/log/note.txt
hello_sfs_tail

> stat note.txt
SMARTFS: STAT path=/log/note.txt type=file size=14

> cd /
SMARTFS: CWD /

> ls
SMARTFS: LS /
dir     14      log
SMARTFS: LS done count=1

> stat /log
SMARTFS: STAT path=/log type=dir size=14

> rm /log/note.txt
SMARTFS: RM OK path=/log/note.txt

> rm /log
SMARTFS: RM OK path=/log

> df
SMARTFS: DF total=2097152 used=32768 free=2064384 block=4096 blocks=512 used_blocks=8 cwd=/log
```

如果串口工具发送后 App 没有进入升级，请按下面顺序观察：

| 顺序 | 观察口 | 期望现象 |
|---|---|---|
| 1 | `RS485/USART1` | 板子上电时能看到一次 `OTA485: ready, send Project_ota.bin raw` |
| 2 | `USART0` (`PA9/PA10`) | 开始发送后出现 `OTA: rx chunk len=... state=... received=...` |
| 3 | `USART0` (`PA9/PA10`) | 若出现 `OTA: bad header status=...`，优先确认发送的是 `Project_ota.bin` 且串口工具为二进制原始发送；修正后可直接从文件开头重发，看到 `OTA: resync after error code=...` 表示已重新同步 |
| 4 | `USART0` (`PA9/PA10`) | 若出现 `OTA: payload crc fail` 或 `OTA: download crc fail`，优先查传输丢字节、Flash 写入和分区常量 |
| 5 | 两路都无输出 | 优先查是否仍在运行旧 App，或接线/串口号并未落在 `RS485/USART1` |

### 8.4 添加自定义任务

1. 在 `Function/` 目录创建 `my_app.c` 和 `my_app.h`
2. 实现任务函数:
```c
#include "mcu_cmic_gd32f470vet6.h"

void my_task(void)
{
    // 你的代码
}
```
3. 在 `scheduler.c` 中添加任务:
```c
static task_t scheduler_task[] = {
    // ... 已有任务
    {my_task, 100, 0},  // 100ms 周期
};
```
4. 在 `mcu_cmic_gd32f470vet6.h` 中添加函数声明
5. 重新编译

---

## 9. 注意事项

1. **DMA 通道冲突**: 使用新外设前请检查 DMA 通道映射，避免冲突
2. **低功耗按键**: KEY1=Sleep，KEY2=Deep-sleep，KEY3=Standby 准备态；KEY3 后按下并松开 KEY4 确认进入，Sleep/Deep-sleep/Standby 均使用 KEYW(PA0) 作为唤醒键
3. **OLED 刷新率**: OLED 任务 100ms 执行一次，应用层带脏行缓存，底层使用页级批量写入以降低 I2C 事务开销
4. **printf 缓冲区**: `my_printf` 使用 512 字节缓冲区；`oled_printf` 使用 32 字节行缓冲并裁剪到 16 个可见字符
